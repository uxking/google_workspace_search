# Google Workspace Marketplace Search #

Using a Programmable Search Engine - search google for marketplace apps based on their ID.

`final_output.py` is used to create a csv - used a `#` as a delimiter.
